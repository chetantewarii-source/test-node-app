# apiVersion: v1
# kind: Service
# metadata:
#   name: node-service
# spec:
#   type: NodePort
#   selector:
#     app: node-app
#   ports:
#     - port: 3000
#       targetPort: 3000
#       nodePort: 30007

apiVersion: v1
kind: Service
metadata:
  name: square-node-service
spec:
  type: NodePort
  selector:
    app: square-node-app
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30008